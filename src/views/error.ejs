<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || '错误' %> - 自动售货系统</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-6 text-center">
        <div class="mb-4">
          <% if (code === 404) { %>
            <i class="bi bi-search display-1 text-muted"></i>
          <% } else if (code === 403) { %>
            <i class="bi bi-shield-x display-1 text-danger"></i>
          <% } else if (code === 500) { %>
            <i class="bi bi-exclamation-triangle display-1 text-warning"></i>
          <% } else { %>
            <i class="bi bi-emoji-frown display-1 text-muted"></i>
          <% } %>
        </div>
        
        <h1 class="display-4 mb-3"><%= code || 'Error' %></h1>
        
        <p class="lead text-muted mb-4">
          <%= message || '发生了错误' %>
        </p>

        <% if (code === 404) { %>
          <p class="text-muted mb-4">您访问的页面不存在或已被删除</p>
        <% } else if (code === 403) { %>
          <p class="text-muted mb-4">您没有权限访问此页面</p>
        <% } else if (code === 500) { %>
          <p class="text-muted mb-4">服务器遇到问题，请稍后重试</p>
        <% } else { %>
          <p class="text-muted mb-4">请稍后重试或联系管理员</p>
        <% } %>

        <div class="d-flex justify-content-center gap-3">
          <a href="/" class="btn btn-primary">
            <i class="bi bi-house me-2"></i>返回首页
          </a>
          <button onclick="history.back()" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left me-2"></i>返回上页
          </button>
        </div>

        <% if (typeof stack !== 'undefined' && stack) { %>
          <div class="mt-5 text-start">
            <div class="card border-danger">
              <div class="card-header bg-danger text-white">
                <i class="bi bi-bug me-2"></i>开发模式 - 错误详情
              </div>
              <div class="card-body">
                <pre class="mb-0 small text-danger" style="white-space: pre-wrap; word-break: break-all;"><%= stack %></pre>
              </div>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
